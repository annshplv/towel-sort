
// You should implement your task here.

module.exports = function towelSort (matrix) {
    for (let j = 0; j < matrix[i].length; j++) {
        let columnInd = i % 2 === 0
        ? j
        : (matrix[i].length - j - 1);
    }
  return [matrix[i][columnInd]];
}
